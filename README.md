React Todo App using json-server to make http requests.

Instructions:
Step 1: "npm install" to install the dependencies,
Step 2: "npm install json-server",
Step 3: "json-server --watch data/db.json --port 8000" to start the json server,
Step 4:Open another cmd, and run "npm start" to start the app.
